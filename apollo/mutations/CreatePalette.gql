mutation createPalette($title: String!, $colors: [String!]!, $userId: ID!) {
  createPalette(title: $title, colors: $colors, userId: $userId) {
    id
    title
    colors
    user {
      id
    }
  }
}
